<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rank">


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activity.Login"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ForgotPassword"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activity.SignUp"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activity.MainActivity"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".Adapter.Chat" />
        <activity android:name=".activity.Communities" />
        <activity android:name=".activity.Chef" />
        <activity android:name=".activity.Notifications" />
        <activity android:name=".activity.Profile" />
        <activity android:name=".activity.Post" />
        <activity android:name=".activity.Comment"></activity>
    </application>

</manifest>